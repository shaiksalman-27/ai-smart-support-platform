[project]
name = "ai-smart-support"
version = "0.1.0"
description = "AI Smart Support & Cybersecurity Assistant"
requires-python = ">=3.10"
dependencies = [
  "openenv-core>=0.2.0",
  "requests",
]

[project.scripts]
server = "server.app:main"